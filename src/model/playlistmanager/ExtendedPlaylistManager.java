package model.playlistmanager;

/**
 * This interface is used for aggregate two another interface 
 * @author Matteo Gabellini
 *
 * @param <X> the element type managed by the class that implements this interface 
 */
public interface ExtendedPlaylistManager<X> extends PlaylistManager<X>,PlaylistFeatures{

}
