package Model;
/**
 * This enum rappresent all possible state that a player can assume
 * @author Matteo Gabellini
 *
 */

public enum PlayerState {
	RUNNING, PAUSED, STOPPED, ERROR, SONGCHANGED, RELOAD;
}
